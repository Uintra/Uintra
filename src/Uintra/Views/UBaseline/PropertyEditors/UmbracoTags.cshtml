@using Compent.Shared.Extensions.Bcl
@model UBaseline.Core.IndexPage.PropertyEditorContentModel

@{
    var value = Model.Value<string[]>();
}

@if (value != null && value.HasValue())
{
    <div>
        <ul>
            @foreach (var str in value)
            {
                <li>
                    @str
                </li>
            }
        </ul>
    </div>
}
