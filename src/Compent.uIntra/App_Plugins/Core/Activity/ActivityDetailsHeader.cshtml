@using uIntra.Core.Extentions
@model uIntra.Core.Activity.IntranetActivityDetailsHeaderViewModel
@{
    var profilePageUrl = ViewData.GetProfilePageUrl(Model.Creator.Id);
    var inactiveUser = Model.Creator.Inactive ? "_inactive" : String.Empty;
}

<header class="feed__item-heading clearfix @inactiveUser">
    @Html.Action("Photo", "User", new { user = Model.Creator })
    <div class="feed__item-title-holder">
        <p class="feed__item-meta">
            @foreach (var date in Model.Dates)
            {
                <span class="feed__item-date">@date</span>
                <span class="feed__item-divider"> - </span>
            }
            <span class="feed__item-category">@Html.Localize(Model.Type.Name)@(Model.Creator != null ? "," : string.Empty)</span>
            @if (Model.Creator != null)
            {
                <a href="@profilePageUrl">
                    <span class="feed__item-name">@Model.Creator.DisplayedName</span>
                </a>
            }
            <span class="feed__item-inactive">[@Html.Localize("ActivityItemHeader.Creator.Inactive.lbl")]</span>
        </p>
    </div>
</header>
@if (Model.Title.IsNotNullOrEmpty())
{
    <h2 class="feed__item-title">@Model.Title</h2>    
}