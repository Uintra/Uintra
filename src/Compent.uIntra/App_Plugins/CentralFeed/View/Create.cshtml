@model Uintra.CentralFeed.CreateViewModel
@{ 
    if (string.IsNullOrWhiteSpace(Model.Settings.Controller))
    {
        return;
    }
    Html.RenderAction("Create", Model.Settings.Controller, new
    {
        links = Model.Links
    });

}