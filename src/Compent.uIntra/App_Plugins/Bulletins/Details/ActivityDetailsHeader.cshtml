@using uIntra.Core.Extentions
@model uIntra.Core.Activity.IntranetActivityDetailsHeaderViewModel

@{
    var defaultImage = "/Content/images/default-avatar.png";
}

<header class="feed__item-heading clearfix">
    <div class="avatar">
        @if (!String.IsNullOrEmpty(Model.Creator.Photo))
            {
            <img class="avatar__img" style="width: 60px; height: 60px;" src="@(Model.Creator?.Photo)" />
        }
        else
        {
            <img class="avatar__img" src="@defaultImage" />
        }
    </div>
    <div class="feed__item-title-holder">
        <p class="feed__item-meta">
            @foreach (var date in Model.Dates)
            {
                <span class="feed__item-date">@date</span>
                <span class="feed__item-divider"> - </span>
            }
            <span class="feed__item-category">@Html.Localize(Model.Type)</span>
        </p>
    </div>
</header>