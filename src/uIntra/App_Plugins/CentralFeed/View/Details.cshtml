@using System.Web.Mvc.Html
@model uIntra.CentralFeed.DetailsViewModel
@{ Html.RenderAction("Details", Model.Settings.Controller, new
   {
       id = Model.Id,
       options = Model.Options
   }); 

}
