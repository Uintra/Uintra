require('./../Css/tableEditor.css');

var controller = {
    init: function () {        
        var selector = '.js-table-panel';
        var holders = $(selector);
        if (!holders.length) {
            return;
        }
    }
}

export default controller