@using uCommunity.Core
@using uCommunity.Core.Activity
@using uCommunity.Core.Extentions
@using uCommunity.Notification.Core.Configuration

@model uCommunity.Notification.Core.Models.NotificationListViewModel

<div class="row">
    <div class="col-md-6">
        <section class="notifications">
            <ul class="notifications__list" id="notification_list">
                @foreach (var notification in Model.Notifications)
                {
                    switch (notification.Type)
                    {
                        case NotificationTypeEnum.ActivityLikeAdded:
                            {
                                <li class="notifications__list-item" data-id="@notification.Id" data-viewed="@notification.IsViewed.ToString().ToLower()">
                                    <h2 class="notifications__list-title">
                                        <a class="notifications__list-link" href="@notification.Value.url">
                                            @notification.Value.title - @Html.Localize((IntranetActivityTypeEnum)notification.Value.activityType)
                                        </a>
                                    </h2>
                                    <div class="notifications__list-meta">
                                        <span>@notification.Date.ToString(IntranetConstants.Common.DefaultDateTimeFormat)</span> - <span>@notification.Value.notifierName</span> - <span>@notification.Type</span>
                                    </div>
                                </li>

                                break;
                            }

                        case NotificationTypeEnum.CommentLikeAdded:
                            {
                                <li class="notifications__list-item" data-id="@notification.Id" data-viewed="@notification.IsViewed.ToString().ToLower()">
                                    <h2 class="notifications__list-title">
                                        <a class="notifications__list-link" href="@notification.Value.url">
                                            @notification.Value.title - @Html.Localize((IntranetActivityTypeEnum)notification.Value.activityType)
                                        </a>
                                    </h2>
                                    <div class="notifications__list-meta">
                                        <span>@notification.Date.ToString(IntranetConstants.Common.DefaultDateTimeFormat)</span> - <span>@notification.Value.notifierName</span> - <span>@notification.Type</span>
                                    </div>
                                </li>

                                break;
                            }

                        case NotificationTypeEnum.EventUpdated:
                        case NotificationTypeEnum.EventHided:
                        case NotificationTypeEnum.BeforeStart:
                            {
                                <li class="notifications__list-item" data-id="@notification.Id" data-viewed="@notification.IsViewed.ToString().ToLower()">
                                    <h2 class="notifications__list-title">
                                        <a class="notifications__list-link" href="@notification.Value.url">
                                            @notification.Value.title
                                        </a>
                                    </h2>
                                    <div class="notifications__list-meta">
                                        <span>@notification.Date.ToString(IntranetConstants.Common.DefaultDateTimeFormat)</span> - <span>@notification.Type</span> - <span>@notification.Value.notifierName</span>
                                    </div>
                                </li>
                                break;
                            }

                        case NotificationTypeEnum.CommentAdded:
                        case NotificationTypeEnum.CommentEdited:
                        case NotificationTypeEnum.CommentReplyed:
                            {
                                <li class="notifications__list-item" data-id="@notification.Id" data-viewed="@notification.IsViewed.ToString().ToLower()">
                                    <h2 class="notifications__list-title">
                                        <a class="notifications__list-link" href="@notification.Value.url">
                                            @notification.Value.title
                                        </a>
                                    </h2>
                                    <div class="notifications__list-meta">
                                        <span>@notification.Date.ToString(IntranetConstants.Common.DefaultDateTimeFormat)</span> <span>@notification.Value.notifierName</span> - <span>@notification.Type</span>
                                    </div>
                                </li>
                                break;
                            }
                    }
                }
            </ul>
        </section>
    </div>
</div>