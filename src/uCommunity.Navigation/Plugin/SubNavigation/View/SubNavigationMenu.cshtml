@model uCommunity.Navigation.DefaultImplementation.SubNavigationMenuViewModel

<nav class="tabset">
    <strong class="tabset__logo"><a href="/"><img class="img-responsive" src="~/Content/images/logo.png" alt="Team Denmark"></a></strong>
    <a id="js-sidepanel-opener" class="sidebar__opener _for-mobile"></a>
    @if (Model != null)
    {
        <div class="tabset__holder">
            
            @if (Model.Parent != null)
            {
                <h1 class="tabset__title"><a class="tabset__title-link" href="@Model.Parent.Url">@Model.Parent.Name</a></h1>
            }
            else
            {
                <h1 class="tabset__title">@Model.Title</h1>
            }

            @if (Model.Items.Count > 0)
            {
                <ul class="tabset__navigation clearfix">
                    @foreach (var item in Model.Items)
                    {
                        <li class="tabset__navigation-item @(item.IsActive ? "_active" : "")">
                            <a class="tabset__navigation-link" href="@item.Url">@item.Name</a>
                        </li>
                    }
                </ul>
            }
        </div>
    }
</nav>